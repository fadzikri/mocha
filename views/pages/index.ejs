<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Mocha Home">
    <link rel="shortcut icon" href="favicon.ico" type="image/x-icon">
    <title>Mocha Home</title>
    <style>
        @media (prefers-color-scheme: dark) {
            body {
                background-color: #2E3440;
                color: #ECEFF4;
            }
        @media (prefers-color-scheme: light) {
            body {
                background-color: #ECEFF4;
                color: #2E3440;
            }
    </style>
</head>
<body>
    <pre><% for (const property in headersOne) { %>
 <%- `${property.replace(/^\w|-\w/g, (c) => c.toUpperCase())}: ${headersOne[property]}` %><% } %><% for (const property in headersTwo) { %>
   <%- `${property.replace(/^\w|-\w/g, (c) => c.toUpperCase())}: ${headersTwo[property]}` %><% } %>
    </pre>
</body>
</html>
